{
  "name": "undoku",
  "version": "1.0.0",
  "description": "High-Performance Sudoku Generator, Evaluator, Web Canvas Engine & Express Server",
  "scripts": {
    "start": "npm --prefix server start",
    "start:go": "RUN_HTTP_SERVER=true go run main.go",
    "test": "npm --prefix server test && go test -v ./...",
    "test:server": "npm --prefix server test",
    "test:go": "go test -v ./...",
    "dev": "npm --prefix wiki run dev",
    "build": "node scripts/build.js",
    "build:wiki": "npm --prefix wiki run build",
    "deploy": "./scripts/deploy.sh"
  }
}
